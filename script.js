const form=document.getElementById('login-form');const message=document.getElementById('message');const success=document.getElementById('success-screen');const closeSuccess=document.getElementById('close-success');form.addEventListener('submit',e=>{e.preventDefault();const id=document.getElementById('identifiant').value.trim().toUpperCase();const pw=document.getElementById('password').value.trim().toUpperCase();if(id!=='JQ-0731'){message.textContent='Identifiant non reconnu.';return}if(pw!=='JQTV'){message.textContent='Clé d’accès incorrecte.';return}message.textContent='Authentification en cours…';setTimeout(()=>{success.hidden=false;message.textContent=''},900)});closeSuccess.addEventListener('click',()=>{success.hidden=true});
